#criar uma lista vazia(mochila)

#criar a lista com os itens(itens)
#criar a lista com os pesos(pesos)

#criar uma variavel com o valor 0(pesoTotal)

#fazer um looping até o len de itens com a variavel(i)

    #se a soma de pesoTotal com o valor pelo qual i vale em pesos for menor ou igual a 5
        #mochila vai receber o valor pelo qual i representa em itens
        #pesoTotal vai somar com o valor pelo qual i vale em pesos
